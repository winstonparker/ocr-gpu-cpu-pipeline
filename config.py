# Default Configs. Can be changed via the CLI.
CONFIG = {
    "pdf_dir": "./input",
    "output_dir": "./output",
    "temp_dir": "./temp_images",
    "max_pdfs_per_batch": 10,
    "preprocess_batch_size": 100,
    "num_producers": 10,
    "num_consumers": 15,
    "image_dpi": 300,
    "preview_image": False,
    "tessdata_prefix": "",
}

VERSION = "1.0"